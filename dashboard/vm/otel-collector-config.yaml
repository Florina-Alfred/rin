receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  prometheus:
    endpoint: "victoriametrics:8428"
    namespace: "otel"
  
service:
  pipelines:
    metrics:
      receivers: [otlp]
      exporters: [prometheus]

